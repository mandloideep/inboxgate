# Known accepted risks

This register records unresolved security and reliability risks that the repository owner has explicitly accepted so work can continue.
An accepted risk is not a fixed risk and must not be described as a security control.

The current acceptance applies only to the exact dependency version and adapter selected by [ADR 0004](adr/0004-turso-serverless-adapter.md), as extended for credential-free loopback migrations by [ADR 0005](adr/0005-append-only-migration-protocol.md), minimum account-cursor persistence by [ADR 0006](adr/0006-minimum-account-cursor-persistence.md), ciphertext-only provider-credential persistence by [ADR 0007](adr/0007-versioned-provider-credential-encryption.md), synthetic OAuth enrollment reconciliation by [ADR 0008](adr/0008-google-oauth2-client.md), account lifecycle persistence by [ADR 0009](adr/0009-account-lifecycle-and-revocation.md), and atomic current-discovery staging by [ADR 0010](adr/0010-atomic-current-discovery-staging.md).
It does not authorize production credentials, runtime activation, remote migrations, live account data, plaintext provider data, live email metadata, or live Turso Cloud access.

## Register

| ID | Affected version | Owner acceptance date | Impact | Existing containment | Missing upstream control | Revisit trigger | Closure criteria |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `TURSO-001` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | A malicious or compromised server response can replace the scheme or authority in `base_url`, after which a later request can send the bearer token to a different authority or over cleartext HTTP. | Migration, account-cursor, ciphertext-credential, lifecycle, and current-discovery execution reject credentials and non-loopback endpoints before connection acquisition, and credential-free regressions record the driver's changed-authority behavior, while same-authority and no-downgrade enforcement remain unavailable. | Same-authority and no-downgrade validation for protocol-provided URLs, or an injectable authority and scheme policy enforced before credentials are attached. | Driver upgrade, upstream authority-control change, runtime activation, credential use, or any new query path. | Before live credentials are permitted, credential-free regressions must prove that a cross-authority response cannot receive a bearer token and that a protocol-provided URL cannot downgrade a later bearer-bearing request from HTTPS to HTTP, with the enforcing controls owned by InboxGate or explicitly exposed by the selected dependency. |
| `TURSO-002` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | Remote HTTP and protocol messages can contain secrets, SQL, values, paths, URLs, account identities, cursor values, ciphertext envelopes, lifecycle values, email metadata, attempt identifiers, or other private data and can be reflected by the driver error. | The adapter replaces ping, migration, account, cursor, credential, lifecycle, deletion, current-discovery, and returned close failures with fixed categories, preserves only standard context cancellation identity, validates every decoded account, cursor, key identifier, ciphertext envelope, lifecycle value, summary, canonical message, attempt, and staged row, and does not expose the SDK error. | Bounded typed errors that separate machine-readable classification from untrusted remote text across every future operation. | Driver upgrade, logging change, runtime activation, or any operation beyond the current loopback surface. | Every reachable adapter operation maps failures to bounded typed diagnostics, and regressions prove remote bodies, SQL, arguments, tokens, URLs, paths, account data, cursor values, ciphertext envelopes, lifecycle values, email metadata, and staging values cannot cross the boundary. |
| `TURSO-003` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | Commit, rollback, and stream close can outlive the caller's deadline, hold resources, and leave transaction outcome uncertain. | Migrations retain their atomic sequence and terminal proof controls. Account creation, cursor commits, credential commits, lifecycle commits, revoked credential deletion, and each current-discovery staging or finalization step use at most one fixed implicit-transaction statement, keep the mutation connection reserved while a separate physical connection proves exact durable state, discard an unproven mutation session, never replay within an invocation, and require a fresh explicit invocation to reconcile uncertain outcomes. Revocation requires a proven pending-to-attempting claim before its one provider crossing, and restart from attempting fails closed to manual action without a second provider call. Current discovery uses durable bounded staging and a single trigger-backed finalization statement so a fresh invocation can inspect the complete outcome without fetching Gmail again. Complete invocation and stream close are not claimed bounded. | Caller-controlled contexts for every driver close path, or an owned bounded client that cancels the underlying request. | Driver upgrade, shutdown integration, runtime activation, or any broader persistence path. | End-to-end stalled-server tests prove commit, rollback, trigger finalization, and close cancel the underlying request within an owned deadline without abandoning a live request or misreporting transaction outcome. |
| `TURSO-004` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | The private HTTP client prevents InboxGate from enforcing redirect rejection, transport timeouts, connection policy, and test transport injection. | Migration, account-cursor, ciphertext-credential, lifecycle, and current-discovery execution are credential-free and literal-loopback only, operation contexts are bounded, and credential-free regressions record that the private client follows a redirect, while redirect and transport policy remain unavailable. | An injectable client or transport with fail-closed redirect, TLS, dial, connection, and total-operation policies. | Driver upgrade, private endpoint design, network policy design, runtime activation, or any live credential plan. | InboxGate owns or configures the client policy, and regressions prove redirects fail closed before authorization data reaches another authority. |
| `TURSO-005` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | A successful response can consume unbounded memory through a pipeline body, cursor line, accumulated rows, or individual value. | The embedded catalog and post-decode migration, account, cursor, ciphertext-credential, lifecycle, listing, current-discovery attempt, manifest witness, staging, chunk-proof, record-key, natural-key, and canonical-message logic bound accepted rows, columns, types, identifiers, subjects, history IDs, key identifiers, envelopes, lifecycle values, canonical metadata, aggregate bytes, summaries, and results, and oversized semantic regressions prove rejection after driver buffering. | Repository-owned limits for total body bytes, cursor-line bytes, accumulated rows, and individual value bytes before driver allocation, or equivalent bounded streaming hooks. | Driver upgrade, repository query design, runtime activation, or any broader data-returning operation. | Oversized successful-response tests prove every relevant byte, line, row, and value limit fails safely before unbounded allocation within documented memory and disk bounds. |

## Review procedure

Every direct driver upgrade must compare the exact source behavior against all open risk IDs.
The review must update the affected version, evidence, containment, and closure state before merge.

Any issue that makes the adapter runtime-reachable must list every open risk ID in its security impact and explain why the proposed use stays within the owner's acceptance.
If a feature needs behavior outside the current containment, stop and obtain a new explicit decision rather than treating this register as blanket approval.

Close a risk only after a focused issue supplies reproducible evidence for its closure criteria and updates the ADR, threat model, tests, and this register.
