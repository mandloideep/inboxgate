# Known accepted risks

This register records unresolved security and reliability risks that the repository owner has explicitly accepted so work can continue.
An accepted risk is not a fixed risk and must not be described as a security control.

The current acceptance applies only to the exact dependency version and inert adapter selected by [ADR 0004](adr/0004-turso-serverless-adapter.md).
It does not authorize production credentials, runtime activation, migrations, stored data, or live Turso Cloud access.

## Register

| ID | Affected version | Owner acceptance date | Impact | Existing containment | Missing upstream control | Revisit trigger | Closure criteria |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `TURSO-001` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | A malicious or compromised server response can replace the scheme or authority in `base_url`, after which a later request can send the bearer token to a different authority or over cleartext HTTP. | The adapter rejects credentials in the initial URL, requires the initial remote endpoint to use HTTPS, permits initial cleartext only on literal loopback without a token, and is not runtime-active, while not validating a later protocol-provided `base_url` or preventing that URL from downgrading HTTPS to HTTP. | Same-authority and no-downgrade validation for protocol-provided URLs, or an injectable authority and scheme policy enforced before credentials are attached. | Driver upgrade, upstream authority-control change, runtime activation, credential use, or any new query path. | Before live credentials are permitted, credential-free regressions must prove that a cross-authority response cannot receive a bearer token and that a protocol-provided URL cannot downgrade a later bearer-bearing request from HTTPS to HTTP, with the enforcing controls owned by InboxGate or explicitly exposed by the selected dependency. |
| `TURSO-002` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | Remote HTTP and protocol messages can contain secrets, SQL, values, paths, URLs, or private data and can be reflected by the driver error. | The current adapter replaces ping and returned close failures with fixed categories and does not expose the SDK error. | Bounded typed errors that separate machine-readable classification from untrusted remote text across every operation. | Driver upgrade, logging change, runtime activation, or any operation beyond ping and close. | Every reachable adapter operation maps failures to bounded typed diagnostics, and regressions prove remote bodies, SQL, arguments, tokens, URLs, and paths cannot cross the boundary. |
| `TURSO-003` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | Commit, rollback, and stream close can outlive the caller's deadline, hold resources, and leave transaction outcome uncertain. | Ping uses an adapter-owned deadline, close is invoked only once, and no transaction or persistence path is active. | Caller-controlled contexts for commit, rollback, and close, or an owned bounded client that cancels the underlying request. | Driver upgrade, transaction design, migration work, shutdown integration, or runtime activation. | End-to-end stalled-server tests prove commit, rollback, and close cancel the underlying request within an owned deadline without abandoning a live request or misreporting transaction outcome. |
| `TURSO-004` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | The private HTTP client prevents InboxGate from enforcing redirect rejection, transport timeouts, connection policy, and test transport injection. | The adapter validates only the caller-supplied initial endpoint, requires standard verified HTTPS for that remote endpoint, limits initial cleartext to credential-free literal loopback, and remains inert, while later protocol-driven scheme and authority changes remain under `TURSO-001`. | An injectable client or transport with fail-closed redirect, TLS, dial, connection, and total-operation policies. | Driver upgrade, private endpoint design, network policy design, runtime activation, or any live credential plan. | InboxGate owns or configures the client policy, and regressions prove redirects fail closed before authorization data reaches another authority. |
| `TURSO-005` | `tursogo-serverless v0.0.0-20260817122138-24adc316cdc4` | 2026-08-17 | A successful response can consume unbounded memory through a pipeline body, cursor line, accumulated rows, or individual value. | The current adapter issues only a fixed `SELECT 1` ping, gives it a deadline, exposes no query method, and is not runtime-active. | Repository-owned limits for total body bytes, cursor-line bytes, row count, and individual value bytes, or equivalent bounded streaming hooks. | Driver upgrade, migration work, repository query design, runtime activation, or any data-returning operation. | Oversized successful-response tests prove every relevant byte, line, row, and value limit fails safely within documented memory and disk bounds. |

## Review procedure

Every direct driver upgrade must compare the exact source behavior against all open risk IDs.
The review must update the affected version, evidence, containment, and closure state before merge.

Any issue that makes the adapter runtime-reachable must list every open risk ID in its security impact and explain why the proposed use stays within the owner's acceptance.
If a feature needs behavior outside the current containment, stop and obtain a new explicit decision rather than treating this register as blanket approval.

Close a risk only after a focused issue supplies reproducible evidence for its closure criteria and updates the ADR, threat model, tests, and this register.
